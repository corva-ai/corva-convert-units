name: 'Code Analysis'

on:
  pull_request:
    types:
      - opened
      - synchronize
  push:
    branches:
      - master

permissions:
  id-token: write
  contents: read
  actions: read
  checks: write
  statuses: write

jobs:
  test-js:
    name: JS Tests
    runs-on: ubuntu-24.04
    steps:
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v3.0.0
        id: secrets
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN_RO }}
          APP_ID: 'op://GitHub.Actions/GitHub/corva-bot/APP_ID'
          PRIVATE_KEY: 'op://GitHub.Actions/GitHub/corva-bot/PRIVATE_KEY'
      - name: Checkout sources
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - name: Setup Environment
        uses: ./.github/actions/setup-environment
      - name: Cache ESLint
        uses: actions/cache@v4.2.2
        with:
          path: .eslintcache
          key: ${{ runner.os }}-eslint-${{ hashFiles('js/package-lock.json') }}-${{ hashFiles('./.eslint*') }}
      - name: Lint
        run: yarn lint
      - name: Generate GitHub token
        uses: actions/create-github-app-token@v1.11.6
        if: ${{ success() || failure() }}
        id: token
        with:
          app-id: ${{ steps.secrets.outputs.APP_ID }}
          private-key: ${{ steps.secrets.outputs.PRIVATE_KEY }}
      - name: Add PR status check
        uses: ouzi-dev/commit-status-updater@v2.0.2
        if: ${{ success() || failure() }}
        with:
          token: ${{ steps.token.outputs.token }}
          name: 'Lint'
          status: ${{ job.status }}
          url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          description: ${{ job.status == 'success' && 'Linted successfully' || 'Linted with errors. Check PR annotations' }}

  test:
    name: Unit Test
    runs-on: ubuntu-24.04
    steps:
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v3.0.0
        id: secrets
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN_RO }}
          #
          APP_ID: 'op://GitHub.Actions/GitHub/corva-bot/APP_ID'
          PRIVATE_KEY: 'op://GitHub.Actions/GitHub/corva-bot/PRIVATE_KEY'
      - name: Checkout sources
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - name: Setup Environment
        uses: ./.github/actions/setup-environment
      - name: Cache Jest
        uses: actions/cache@v4.2.2
        with:
          path: js/node_modules/.cache/jest
          key: ${{ runner.os }}-jest-${{ hashFiles('js/package-lock.json') }}
      - name: Sync definitions
        uses: ./.github/actions/sync-definitions
      - name: Test
        run: cd js && npm test
      - name: Generate GitHub token
        uses: actions/create-github-app-token@v1.11.6
        if: ${{ success() || failure() }}
        id: token
        with:
          app-id: ${{ steps.secrets.outputs.APP_ID }}
          private-key: ${{ steps.secrets.outputs.PRIVATE_KEY }}
      - name: Add PR status check
        uses: ouzi-dev/commit-status-updater@v2.0.2
        if: ${{ success() || failure() }}
        with:
          token: ${{ steps.token.outputs.token }}
          name: 'JS Tests'
          status: ${{ job.status }}
          url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          description: ${{ job.status == 'success' && 'JS tests passed' || 'JS tests failed' }}

  test-py:
    name: Python Tests
    runs-on: ubuntu-24.04
    steps:
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v3.0.0
        id: secrets
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN_RO }}
          APP_ID: 'op://GitHub.Actions/GitHub/corva-bot/APP_ID'
          PRIVATE_KEY: 'op://GitHub.Actions/GitHub/corva-bot/PRIVATE_KEY'
      - name: Checkout sources
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Sync definitions
        uses: ./.github/actions/sync-definitions
      - name: Install dependencies
        run: cd py && pip install -e ".[test]"
      - name: Test
        run: cd py && python -m pytest
      - name: Generate GitHub token
        uses: actions/create-github-app-token@v1.11.6
        if: ${{ success() || failure() }}
        id: token
        with:
          app-id: ${{ steps.secrets.outputs.APP_ID }}
          private-key: ${{ steps.secrets.outputs.PRIVATE_KEY }}
      - name: Add PR status check
        uses: ouzi-dev/commit-status-updater@v2.0.2
        if: ${{ success() || failure() }}
        with:
          token: ${{ steps.token.outputs.token }}
          name: 'Python Tests'
          status: ${{ job.status }}
          url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          description: ${{ job.status == 'success' && 'Python tests passed' || 'Python tests failed' }}
